substitutions:
  _REGION: 'us-east1'
  _REPOSITORY: 'docker-repo'
  _IMAGE: 'nginx-app'
  _TAG: 'v1.03'
  _LOG_BUCKET: 'devops_ci_logs'

steps:
# 1. Build and Push using Kaniko with Reproducible flag
- name: 'gcr.io/kaniko-project/executor:latest'
  id: 'build-and-push'
  args:
    - --context=dir://app
    - --destination=${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPOSITORY}/${_IMAGE}:${_TAG}
    - --cache=true
    - --cache-ttl=168h
    - --reproducible=true  # <--- CRITICAL: Strips timestamps to keep SHA-256 consistent

# 2. Inspect the image to verify RootFS layers (Focusing on the layers list)
- name: 'gcr.io/cloud-builders/docker'
  id: 'inspect-image'
  entrypoint: 'bash'
  args:
    - '-c'
    - |
      docker pull ${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPOSITORY}/${_IMAGE}:${_TAG}
      echo "--- ROOTFS LAYERS ---"
      docker inspect --format='{{json .RootFS.Layers}}' ${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPOSITORY}/${_IMAGE}:${_TAG}

# 3. Run functional tests
- name: 'gcr.io/cloud-builders/docker'
  id: 'test-image'
  args: ['run', '--rm', '${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPOSITORY}/${_IMAGE}:${_TAG}', 'echo', 'Tests passed']

logsBucket: 'gs://${_LOG_BUCKET}'



# 1. Build the Docker image first (so we have local layers to inspect)
# - name: 'gcr.io/cloud-builders/docker'
#   id: 'build-image'
#   dir: 'app'
#   args: 
#   - 'build'
#   - '-t'
#   - '${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPOSITORY}/${_IMAGE}:${_TAG}'
#   - '.'

# 2. Check if the newly built layers already exist in the Registry

# - name: gcr.io/google.com/cloudsdktool/cloud-sdk
#   id: 'check-layers'
#   entrypoint: 'bash'
#   args:

#   - '-c'
#   - |
    
    # # Define the image you just built

    # NEW_IMAGE='${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPOSITORY}/${_IMAGE}:${_TAG}'

    # # 1. Get the layers of the NEW image (as a single string of hashes)
    
    # NEW_LAYERS=$$(docker inspect --format='{{range .RootFS.Layers}}{{println .}}{{end}}' "$$NEW_IMAGE")

    # if [ -z "$$NEW_LAYERS" ]; then
    #     echo "Error: Could not find layers for $$NEW_IMAGE. Does it exist?"
    #     exit 1
    # fi

    # echo "--------------------------------------------------------"
    # echo "Comparing content layers of: $$NEW_IMAGE"
    # echo "--------------------------------------------------------"

    # # 2. Get a list of all other image IDs (excluding the new one itself)
    # # We use 'docker images -q' to get IDs and 'grep -v' to skip the new image's ID

    # NEW_IMAGE_ID=$$(docker inspect --format='{{.Id}}' "$$NEW_IMAGE")
    # EXISTING_IMAGE_IDS=$$(docker images -q | grep -v "$${NEW_IMAGE_ID}")

    # EXISTING_IMAGE_IDS=$$(docker images -q)
    # echo "Existing Image ID is: $$NEW_IMAGE_ID"

    # MATCH_FOUND=false

    # for IMG_ID in $$EXISTING_IMAGE_IDS; do
    #     # Get layers of the existing image
    #     CURRENT_IMG_LAYERS=$$(docker inspect --format='{{range .RootFS.Layers}}{{println .}}{{end}}' "$$IMG_ID")
        
    #     # Get the Name/Tag for display purposes
    #     IMG_NAME=$$(docker inspect --format='{{index .RepoTags 0}}' "$$IMG_ID")

    #     # 3. Compare the strings of hashes
    #     if [ "$$NEW_LAYERS" == "$$CURRENT_IMG_LAYERS" ]; then
    #         echo "[MATCH FOUND] Content is identical to: $$IMG_NAME ($$IMG_ID)"
    #         MATCH_FOUND=true

    #         touch /workspace/skip_push
    #         break
    #     fi
    # done

    # if [ "$$MATCH_FOUND" = false ]; then
    #     echo "Result: No images with identical content found. This is a unique build."
    # fi

# 3. Push the Docker image (only if skip file doesn't exist)
# - name: 'gcr.io/cloud-builders/docker'
#   id: 'push-image'
#   # entrypoint: 'bash'
#   args:
#     - '-c'
#     - |
#       # if [ -f /workspace/skip_push ]; then
#       #   echo "Push skipped."
#       # else
#       ['build', '-t', '${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPOSITORY}/${_IMAGE}:${_TAG}', '.']
#       # fi

# # 4. Test the image (only if skip file doesn't exist)
# - name: 'gcr.io/cloud-builders/docker'
#   id: 'test-image'
#   # entrypoint: 'bash'
#   args: 
#   - '-c'
#   - |
#     # if [ -f /workspace/skip_push ]; then
#     #   echo "Tests skipped."
#     # else
#     ['run', '--rm', '${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPOSITORY}/${_IMAGE}:${_TAG}', 'sh', '-c', 'echo "Hostname is: $(hostname)"']
#     # fi

